    put:
      tags:
        - Appointment Write
      description: |
        # Update Operations

      operationId: millenniumDSTU2AppointmentUpdate
      consumes:
      - application/json+fhir
      produces:
      - application/json+fhir
      parameters:
        - name: id
          in: path
          description: |
            The logical resource ID associated with the resource.
          required: true
          type: string
        - name: updateBody
          in: body
          required: true
          schema:
            $ref: "#/definitions/UpdateBody"
        - name: If-Match
          in: header
          required: true
          type: string
      responses: 
        '200':
          description: |
            **OK**
          headers:
            X-Request-Id:
              type: string
              description: Oracle troubleshooting identifier.
            opc-request-id:
              type: string
              description: Oracle troubleshooting identifier.
            ETag:
              type: string
              description: The identifier for a specific version of a resource. This will be formatted as `W/"<etag_value>"` and is used to specify the `If-Match` header value on subsequent updates.
        default:
          $ref: '#/responses/OperationOutcome'

definitions:
  UpdateBody:
    type: object
    required:
    - resourceType
    - status
    - participant
    - participant.actor
    - participant.status
    properties:
      resourceType:
        description: |
          The type of the FHIR resource.
        type: string
        enum:
        - Appointment
      status:
        description: |
          A code representing the overall status of the Appointment. Each of the participants has their own participation status which indicates their involvement in the process, however this status indicates the shared status.
          
          _Notes_
          Must be <code>arrived</code> or <code>cancelled</code>
          
          _Examples_
          
          ````json
          {
            "status": "arrived"
          }
          
          ````
        type: string
      participant:
        description: |
          Participants involved in appointment.
          _Examples_
          
          ````json
          {
            "participant": [
              {
                "actor": {
                  "reference": "Patient/4704007",
                  "display": "Smart, Barney R"
                },
                "status": "accepted"
              }
            ]
          }
          
          ````
        type: array
        items:
          type: object
          properties:
            :
              type: 
            type: object
            properties:
      participant.actor:
        description: |
          A person, location, healthcare service or device that is participating in the appointment.
          _Examples_
          
          ````json
          {
            "actor": {
              "reference": "Patient/4704007",
              "display": "Smart, Barney R"
            }
          }
          
          ````
        type: object
        properties:
          reference:
            type: string
      participant.status:
        description: |
          Participation status of the actor.
          
          _Notes_
          Must be <code>accepted</code>
          
          _Examples_
          
          ````json
          {
            "status": "accepted"
          }
          
          ````
        type: string